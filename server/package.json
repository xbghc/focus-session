{
  "name": "focus-session-server",
  "version": "0.3.5",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.14.0" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/server/src/main.js",
    "admin": "node dist/server/src/admin.js",
    "test": "node --experimental-strip-types --no-warnings=ExperimentalWarning --test test/*.test.ts"
  },
  "dependencies": { "pg": "^8.16.3" },
  "devDependencies": { "@types/node": "^22.20.1", "@types/pg": "^8.15.5", "typescript": "^5.9.3" }
}
